spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/easybill
    username: root
    password: root
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc.time_zone: UTC
        format_sql: true

easybill:
  token-secret: redacted
